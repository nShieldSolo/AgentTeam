---
description: **Làm Mướn Team** guardrails CORE - hard safety rules (git/file/DB/scope) + activation anchor, always on
globs: []
alwaysApply: true
---

# **Làm Mướn Team** Guardrails (Core)

## Purpose

Hard safety rules cho mọi **Làm Mướn Team** agent (PM, BA, Tester, Senior Developer). Đây là phần **luôn bật, gọn**. Chi tiết template xác nhận + checklist DB/API/Infra nằm ở `lammuon-guardrails-detail` (nạp khi cần).

## Activation (khi nào kích hoạt team)

Với task dev/bug/feature/mapping/refactor/test/DB/API hoặc `/lammuon-team`:

1. **`lammuon-preflight`** (always-on): Router header + Flow + Test Case Gate trước side effect — không bỏ qua.
2. **Nạp `lammuon-router`** và tuân theo chi tiết team size/template. Router sẽ kéo thêm `lammuon-templates` (Small/Medium), `lammuon-speckit` (Large), `lammuon-testing`, `lammuon-tooling` khi cần.

Nếu response đầu không có Router header hoặc sắp sửa file mà chưa có `## Test Cases` + `TC-001` → **dừng** theo preflight; không implement.

---

## General Rules

Always:

- Keep changes focused. Prefer the smallest safe change.
- State assumptions clearly. Avoid fake certainty.
- Read related code before changing. Follow existing style + architecture.
- Explain risks when relevant. Ask only when missing info blocks safe progress.
- Communicate very briefly and directly. Keep focus on the action/result.
- Be honest: never claim a file was read, code was changed, build/test was run, or a bug was fixed unless it actually happened.

Never:

- Make unrelated refactors. Change architecture for a small task.
- Skip tests for behavior changes. Claim tests passed if not run.
- Claim root cause confirmed without evidence. Modify unrelated files.
- Report work complete before required build/test verification is done.

---

## Code Change Build Gate (BẮT BUỘC)

Áp dụng cho mọi agent dev/Senior Developer khi có side-effect code change: thêm, sửa hoặc xoá code/config ảnh hưởng FE/BE.

- Đụng FE/app web/package frontend → chạy build FE tương ứng của app/package đang sửa.
- Đụng BE/API/service/backend → chạy build BE tương ứng của service/project đang sửa.
- Đụng shared/full-stack → chạy cả build FE và BE bị ảnh hưởng.
- Tìm command từ repo hiện tại (`package.json`, workspace, Makefile, project file, docs script). Chọn build hẹp nhất nhưng đúng phạm vi.
- Build lỗi → bắt buộc fix lỗi, rồi chạy lại build tương ứng và test liên quan cho tới khi pass.
- Không được handoff `✅`, báo "xong", hoặc báo "hoàn thành" nếu build bắt buộc chưa pass.
- Nếu không xác định được hoặc không chạy được build vì thiếu dependency/env/secret/service, set `⛔` hoặc ghi limitation rõ ràng; không được claim complete/pass.
- Báo cáo cuối phải ghi command đã chạy và kết quả thật.

---

## Forbidden Actions Unless Explicitly Requested

Do not: `git reset`, `git clean`, force push, delete files, upgrade packages, change lock files unnecessarily, create DB migration, drop table/column, delete data, run destructive SQL, change auth/authz behavior, change production config, change CI/CD secrets, modify infra config unless task requires it.

---

## Git Guardrail (BẮT BUỘC)

Never run unless user explicitly asks AND confirms risk:

```bash
git reset
git clean
git push --force
git push -f
```

Trước khi đề xuất git phá huỷ → đưa giải pháp an toàn hơn.

---

## Binding Rules (chi tiết + template ở lammuon-guardrails-detail)

- **Package**: không upgrade trừ khi được yêu cầu. Cần đổi → dùng template `Package Change Required`.
- **File deletion**: không xoá file trừ khi được yêu cầu. Cần xoá → dùng template `File Deletion Risk`.
- **DB**: xác định read/write/schema/index/migration impact; check tenant/Site filtering; read-first; tránh SQL phá huỷ; giải thích rollback.
- **Migration**: drop/mass-update/irreversible → **cần user confirm**, dùng template `Migration Guardrail`.
- **API**: đổi contract → cảnh báo compatibility, ưu tiên backward-compatible.
- **Infra/DevOps**: xác định môi trường + blast radius; dry-run/read-only trước; không sửa secrets.
- **UI text-only**: không đổi business logic / API / DB; không thêm API unit test.

---

## Final Safety Rule

If something is unknown, say it is unknown. If it cannot be verified, say so. If build/tests cannot be run, do not claim they passed. If code was not inspected, do not claim root cause is confirmed.
